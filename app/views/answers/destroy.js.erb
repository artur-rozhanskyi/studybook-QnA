<% if @answer.errors.present? %>
  <%  @answer.errors.full_messages.each do |message| %>
    $('.answers-errors').html('<%= j message %>');
  <% end %>
<% else %>
  $('*[data-answer-id="<%= "#{@answer.id}" %>"]').parents('.answer').remove();
<% end %>
